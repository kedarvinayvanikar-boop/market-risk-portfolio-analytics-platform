# Dashboard Folder

Use this folder for Power BI assets:

- dashboard screenshots
- exported PDF dashboard previews
- `.pbix` file if you later build the report in Power BI Desktop on Windows

The repository already includes dashboard reference images under:

```text
assets/images/
```
