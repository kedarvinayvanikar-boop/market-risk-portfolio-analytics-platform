# Data Folder

`universe.csv` defines the stock universe and simulated portfolio weights.

The generated SQLite database `stocks.db` is not stored in GitHub. It is created locally by running:

```bash
python3 run_pipeline.py
```
